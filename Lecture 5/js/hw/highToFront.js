function capToFront(string){
    string = string.split("")
    let result = []
    string.forEach(char => {
        if(char == char.toUpperCase()) result.push()
    });
    return string
}

console.log(capToFront("hApPy"))